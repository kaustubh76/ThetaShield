# Dashboard

The dashboard will distinguish simulated data from live on-chain data and expose
directional fees, markout filtering, persistence, confidence, Reactive status,
callback evidence, and research comparisons.
